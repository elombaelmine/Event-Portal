import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.iusj.eventcampusportal',
  appName: 'IUSJ Event Portal',
  webDir: 'www'
};

export default config;